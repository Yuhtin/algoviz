slug: bucket-sort
name: Bucket Sort
category: Ordenacao
description: Algoritmo de ordenacao distributiva que divide elementos em buckets e ordena cada bucket individualmente.
difficulty: Media
tags: [Sorting, Distributivo, Linear]

complexity:
  time: O(n+k)
  space: O(n+k)

presets:
  - label: Distribuido
    data: [0.42, 0.32, 0.23, 0.52, 0.25, 0.47, 0.51]
  - label: Concentrado
    data: [0.21, 0.22, 0.23, 0.24, 0.25, 0.26, 0.27]
  - label: Uniforme
    data: [0.1, 0.3, 0.5, 0.7, 0.9, 0.2, 0.4]
  - label: Invertido
    data: [0.9, 0.8, 0.7, 0.6, 0.5, 0.4, 0.3]

concepts:
  function-def:
    label: Definicao
    explanation: Funcao que ordena um array de numeros entre 0 e 1.
  empty-check:
    label: Caso Base
    explanation: Verifica se o array esta vazio para evitar divisao por zero.
  buckets-init:
    label: Inicializacao
    explanation: Cria n buckets vazios, onde n e o tamanho do array.
  distribution:
    label: Distribuicao
    explanation: Distribui cada elemento para o bucket apropriado baseado em seu valor.
  bucket-sort:
    label: Ordenacao
    explanation: Ordena cada bucket individualmente usando o sort nativo.
  concatenation:
    label: Concatenacao
    explanation: Concatena todos os buckets para formar o resultado final.

theory:
  howItWorks:
    title: Como o Bucket Sort Funciona
    content: |
      Bucket Sort e um algoritmo de ordenacao distributiva que funciona dividindo o array em varios "buckets" (baldes). Cada bucket armazena uma faixa de valores, e apos distribuir todos os elementos, cada bucket e ordenado individualmente (geralmente com insertion sort). Finalmente, os buckets sao concatenados para formar o array ordenado.
    steps:
      - Criar n buckets vazios (onde n e o tamanho do array)
      - Distribuir cada elemento para o bucket apropriado baseado em seu valor
      - Ordenar cada bucket individualmente
      - Concatenar todos os buckets em ordem para obter o resultado final

  keyInsight: Bucket Sort e mais eficiente quando os dados estao uniformemente distribuidos no intervalo. E particularmente util para ordenar numeros de ponto flutuante entre 0 e 1.

  commonMistakes:
    - Assumir que sempre sera O(n) - depende da distribuicao dos dados
    - Usar poucos buckets, causando muitos elementos por bucket
    - Nao considerar o custo do algoritmo de ordenacao interno
    - Aplicar em dados nao uniformemente distribuidos sem ajustar os buckets

  bigO:
    time:
      notation: O(n + k)
      explanation: No caso medio, quando os elementos estao uniformemente distribuidos, o tempo e linear. n e o numero de elementos e k e o numero de buckets.
      proof: Distribuicao O(n) para colocar elementos nos buckets. Ordenacao O(n/k * log(n/k)) por bucket, totalizando O(n) quando bem distribuido. Concatenacao O(k).
    space:
      notation: O(n + k)
      explanation: Precisamos de espaco para os n elementos distribuidos em k buckets.
    bestCase:
      notation: O(n + k)
      explanation: Quando elementos estao uniformemente distribuidos entre os buckets.
    worstCase:
      notation: O(n²)
      explanation: Quando todos os elementos vao para o mesmo bucket, degradando para o algoritmo de ordenacao interno.

  comparisons:
    - name: Bucket Sort
      slug: bucket-sort
      time: O(n+k)
      space: O(n+k)
      stable: true
      bestFor: Dados uniformemente distribuidos
    - name: Bubble Sort
      slug: bubble-sort
      time: O(n²)
      space: O(1)
      stable: true
      bestFor: Arrays pequenos
    - name: Counting Sort
      time: O(n+k)
      space: O(k)
      stable: true
      bestFor: Inteiros em faixa conhecida
    - name: Radix Sort
      time: O(d*n)
      space: O(n+k)
      stable: true
      bestFor: Inteiros com digitos fixos

interview:
  - id: bucket-sort-q1
    question: Explique como o Bucket Sort funciona e quando voce o usaria.
    difficulty: medium
    companies: [Google, Amazon, Microsoft]
    tip: Pense em como dividir o problema em partes menores.
    rubric:
      - point: Explica que distribui elementos em buckets baseado em seu valor
        required: true
      - point: Menciona que cada bucket e ordenado individualmente
        required: true
      - point: Explica a concatenacao dos buckets para formar o resultado
        required: true
      - point: Menciona que funciona melhor com dados uniformemente distribuidos
        required: true
      - point: Complexidade O(n+k) no caso medio
        required: false
    idealAnswer: |
      O Bucket Sort e um algoritmo de ordenacao distributiva que funciona da seguinte forma:

      1. Criar buckets: Divide o intervalo de valores em n buckets (geralmente o mesmo numero de elementos do array).

      2. Distribuir elementos: Cada elemento e colocado no bucket apropriado baseado em seu valor. Para numeros entre 0 e 1, o indice do bucket e calculado como floor(valor * n).

      3. Ordenar buckets: Cada bucket e ordenado individualmente usando outro algoritmo (geralmente Insertion Sort).

      4. Concatenar: Os buckets sao concatenados em ordem para formar o array ordenado.

      Quando usar:
      - Dados uniformemente distribuidos em um intervalo conhecido
      - Numeros de ponto flutuante entre 0 e 1
      - Quando a distribuicao permite poucos elementos por bucket

      Complexidade:
      - Tempo: O(n + k) no caso medio (n elementos, k buckets)
      - Espaco: O(n + k) para armazenar os buckets

  - id: bucket-sort-q2
    question: Por que o Bucket Sort pode ter O(n) no caso medio mas O(n²) no pior caso?
    difficulty: medium
    companies: [Facebook, Apple]
    tip: O que acontece quando todos os elementos vao para o mesmo bucket?
    followUp: Como voce poderia mitigar o pior caso?
    rubric:
      - point: Explica que o caso medio assume distribuicao uniforme
        required: true
      - point: Menciona que no pior caso todos os elementos vao para o mesmo bucket
        required: true
      - point: Explica que a ordenacao interna do bucket domina a complexidade
        required: true
      - point: Sugere formas de mitigar (mais buckets, algoritmo interno melhor)
        required: false
    idealAnswer: |
      Por que O(n) no caso medio:
      - Com distribuicao uniforme, cada bucket tem aproximadamente n/k elementos
      - Ordenar cada bucket com Insertion Sort: O((n/k)²)
      - Com k = n buckets, cada bucket tem ~1 elemento
      - Tempo total: O(n) para distribuir + O(k) para concatenar = O(n + k) = O(n)

      Por que O(n²) no pior caso:
      - Se todos os elementos vao para o mesmo bucket (dados nao uniformes)
      - Um bucket com n elementos precisa ser ordenado com Insertion Sort
      - Insertion Sort no pior caso: O(n²)
      - Exemplo: array [0.11, 0.12, 0.13, 0.14, 0.15] com 5 buckets - todos vao para bucket 0

      Mitigacoes:
      1. Usar mais buckets se a distribuicao for desconhecida
      2. Usar algoritmo O(n log n) para ordenar buckets (como Merge Sort)
      3. Analisar a distribuicao dos dados antes de escolher o algoritmo

  - id: bucket-sort-q3
    question: Compare Bucket Sort com Counting Sort e Radix Sort. Quando usar cada um?
    difficulty: hard
    companies: [Google, Microsoft, Uber]
    tip: Pense no tipo de dados que cada algoritmo manipula.
    rubric:
      - point: Counting Sort - inteiros em faixa limitada
        required: true
      - point: Radix Sort - inteiros ou strings com digitos/caracteres fixos
        required: true
      - point: Bucket Sort - numeros de ponto flutuante uniformemente distribuidos
        required: true
      - point: Compara complexidades corretamente
        required: true
      - point: Menciona estabilidade dos algoritmos
        required: false
    idealAnswer: |
      Comparacao:

      | Aspecto | Bucket Sort | Counting Sort | Radix Sort |
      |---------|-------------|---------------|------------|
      | Tipo de dados | Floats uniformes | Inteiros limitados | Inteiros/strings |
      | Complexidade tempo | O(n + k) | O(n + k) | O(d * (n + k)) |
      | Complexidade espaco | O(n + k) | O(k) | O(n + k) |
      | Estavel | Sim* | Sim | Sim |

      Quando usar cada um:

      Counting Sort:
      - Inteiros em faixa conhecida e pequena (ex: idades 0-150)
      - k (range) nao e muito maior que n
      - Precisa de estabilidade
      - Melhor quando k = O(n)

      Radix Sort:
      - Inteiros com numero fixo de digitos
      - Strings de tamanho fixo
      - Quando d (digitos) e pequeno e constante
      - CPFs, telefones, datas

      Bucket Sort:
      - Numeros de ponto flutuante entre 0 e 1
      - Dados uniformemente distribuidos
      - Quando a distribuicao e conhecida
      - Pode ser adaptado para ranges arbitrarios
